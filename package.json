{
  "name": "info2310-project2",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n \"client,server\" -c \"blue.bold,green.bold\" \"npm run dev --workspace=client\" \"npm run dev --workspace=server\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "mongodb": "^6.16.0",
    "mongosh": "^2.5.1",
    "tailwindcss": "^4.1.12"
  }
}
